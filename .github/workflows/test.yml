name: parser tests

on:
  push:
    branches: [ development ]
  pull_request:
    branches: [ main ]

jobs:

  ubuntu_test:
    name: run the test on linux machine
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-latest, windows-latest]
    steps:
    - uses: actions/checkout@v3

    - name: install golang
      uses: actions/setup-go@v3
      with:
        go-version: 1.18

    - name: go test
      run: go test -v -cover ./...
